{
  "name": "rey-midas-digitales",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/gen-sitemap.mjs && npx terser app.js --compress --mangle --output app.js",
    "build:sitemap": "node scripts/gen-sitemap.mjs"
  },
  "devDependencies": {
    "terser": "^5.31.0"
  }
}
